# battery-dispatch

Battery dispatch optimisation across two wholesale electricity markets.

## Setup

```sh
uv sync
```

Setup and usage instructions will be filled in as the model and CLI are built.
